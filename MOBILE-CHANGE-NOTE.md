# Mobile landing change

For phone breakpoints only, `.landing-glorb` is hidden with `display:none!important` and the large top padding previously reserved for the image has been removed.

Desktop and iPad rules were not changed.
